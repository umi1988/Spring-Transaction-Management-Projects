spring:
  application:
    name: Spring-Boot-Transaction-Example
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/order_processing_schema
    username: root
    password: <pwd>
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 9191
